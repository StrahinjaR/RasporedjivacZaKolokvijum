CREATE TABLE [dbo].[Predmeti] (
    [Id]             INT         NOT NULL,
    [Naziv_Predmeta] VARCHAR (1) NOT NULL,
    [Broj_Studenata] INT         NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

